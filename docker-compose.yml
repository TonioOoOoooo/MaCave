services:
  ma-cave:
    build: .
    container_name: ma-cave
    restart: unless-stopped
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?Set ADMIN_PASSWORD in .env}
      DATABASE_URL: file:/app/data/macave.sqlite
      NODE_ENV: production
      PORT: 3015
    ports:
      - "3015:3015"
    volumes:
      - ./data:/app/data
      - ./imports:/app/imports
